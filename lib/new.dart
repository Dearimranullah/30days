import 'package:flutter/material.dart';

class Anayat extends StatelessWidget {
  const Anayat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: const []));
  }
}
